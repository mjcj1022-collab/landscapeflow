[build]
  command = "pnpm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"

# All routes go to index.html so TanStack Router handles client-side navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
